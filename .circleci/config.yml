version: 2.1
jobs:
  build:
    docker:
      - image: circleci/android:api-26-node
        auth:
          username: iarina
          password: W2tDS,whc! 

    steps:
      - checkout

      - run:
          name: Compile the code
          command: arduino-cli compile --fqbn arduino:avr:uno ./problema1.ino

      - store_artifacts:
          path: build

      - store_test_results:
          path: test-results
